package general

// amount of bytes each file chunk will contain when
// uploading or downloading a file.
const CHUNKSIZE int = 1024

// default timeout to use for timeout contexts. defaults
// to 15 (usually seconds).
const DEFAULTTIMEOUT int = 15
